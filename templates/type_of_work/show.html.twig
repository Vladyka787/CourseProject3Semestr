{% extends 'base.html.twig' %}

{% block title %}TypeOfWork{% endblock %}

{% block body %}
    <h1>TypeOfWork</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_of_work.id }}</td>
            </tr>
            <tr>
                <th>TypeOfWorkName</th>
                <td>{{ type_of_work.TypeOfWorkName }}</td>
            </tr>
            <tr>
                <th>TypeOfWorkSkills</th>
                <td>{{ type_of_work.TypeOfWorkSkills }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_of_work_index') }}">back to list</a>

    <a href="{{ path('app_type_of_work_edit', {'id': type_of_work.id}) }}">edit</a>

    {{ include('type_of_work/_delete_form.html.twig') }}
{% endblock %}
