{% extends 'base.html.twig' %}

{% block title %}Редактирование работника{% endblock %}

{% block body %}
    <div class="container-fluid">
        <h1>Редактор работника</h1>

        {{ include('worker/_form.html.twig', {'button_label': 'Обновить'}) }}
        <div class="d-grid gap-2 d-md-block">
            <a href="{{ path('app_worker_index') }}" class="btn btn-outline-primary"
               style="margin-top: 1em; margin-bottom: 1em">К списку работников</a>

            {{ include('worker/_delete_form.html.twig') }}
        </div>

    </div>
{% endblock %}
